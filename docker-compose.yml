services:
  webpage:
    image: nexus.jbrmmg.me.uk:8084/webpage:latest
    container_name: webpage
    restart: unless-stopped
    networks:
      - jbr-network

networks:
  jbr-network:
    external: true
